#include <list>
#include <string>

using namespace std;

class RTTUtil
{
    public:
        double maxRTT(list<double> RTT);
        double minRTT(list<double> RTT);
        double avgRTT(list<double> RTT);
        double stdRTT(list<double> RTT);
    protected:

    private:
};

